# PostgreSQL connection string for the Optiply database.
# Example: postgresql://user:password@host:port/dbname
DATABASE_URL=

# Optional. Set to 1 in production (e.g. Cloud Run) to fail at startup if DATABASE_URL is not set.
# FAIL_FAST_NO_DB=1
