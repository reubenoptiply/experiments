# Python approach (Cloud Run)

Legacy Python engine for the demo account simulator: FastAPI app in `src/` exposing `POST /simulate` and `POST /maintain`, with tests in `tests/`. See the parent [README](../README.md) for setup, run, test, and deploy (build Docker from this directory).
